{% extends "base.html" %}
{% block content %}

	<h1>Select an organisation</h1>
	<ul>
	{% for org in orgs %}
	<li><a href="{{url_for('publisher', id=org.id)}}">{{ org.publisher }}</a></li>
	{% endfor %}
	</ul>
	<h1>Select an element</h1>
	<table class="table">
    <thead>
    <th>Element</th>
    <th>Compliant</th>
    <th>Compliant %</th>
    </thead>
    <tbody>
	{% for element in elements %}
    <tr>
{% set number=0 %}
{% for c in compliance %}
{% if c.id==element.propertyid %}
    {% if c.number is defined %}
    {% set number = c.number %}
    {%endif%}
{%endif%}
{% endfor%}
	<td>{% if element.defining_attribute_value %}<a href="{{url_for('element', id=element.id, type=element.defining_attribute_value)}}">{{element.level|capitalize}} - {{ element.name }} ({{ element.defining_attribute_value }})</a>{% else %}<a href="{{url_for('element', id=element.id)}}">{{element.level|capitalize}} - {{ element.name }}</a>{%endif%} </td>
    <td>{{ number }}</td><td>{{ (number/totalnum*100)|int}}%
</td>
    <td>
    </tr>
	{% endfor %}
	</tbody>
    </table>
{% endblock %}
